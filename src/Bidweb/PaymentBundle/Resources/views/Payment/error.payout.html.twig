{% extends '::landing.html.twig' %}

{% block title %}{{'contact.poster'|trans}} - Bidweb{% endblock%}

{% block body %}
    <div class="col-lg-8">
        <header>
            <h1>{{'payment'|trans}}</h1>
        </header>
        {% if error is defined%}
            {{error}}
        {% else %}
            <p> Your transaction is not done, there is an error. Please try again.</p>
        {% endif %}
        {{errors}}
    </div>
{% endblock %}